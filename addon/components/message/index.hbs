<div
  role="alert"
  class="message message--{{@type}} {{
    if
    @dismissable
    (if this.isDismissed "message--dismissed" "message--revealed")
  }}"
  ...attributes
  {{this.registerElement}}
>
  <div class="message__body">
    {{yield}}
  </div>
  {{#if @dismissable}}
    <button
      type="button"
      class="message__dismiss"
      aria-label="Dismiss"
      {{on "click" this.handleClickDismiss}}
    ></button>
  {{/if}}
</div>